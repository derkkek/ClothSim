---
name: "Custom CodeQL Configuration"

disable-default-queries: false

queries:
  - uses: security-and-quality
  - name: Custom C++ queries
    uses: ./.github/codeql/custom-queries

paths:
  - src
  - Editor/src

paths-ignore:
  - vendor
  - Editor/vendor